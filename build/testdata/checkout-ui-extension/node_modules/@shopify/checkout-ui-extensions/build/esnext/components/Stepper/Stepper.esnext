import { createRemoteComponent } from '@remote-ui/core';

/**
 * A Stepper is used as a quick way for buyers to change a pre-defined number.
 */
export const Stepper = createRemoteComponent('Stepper');