function fromMessagePort(messagePort) {
  return {
    postMessage: function postMessage() {
      return messagePort.postMessage.apply(messagePort, arguments);
    },
    addEventListener: function addEventListener() {
      return messagePort.addEventListener.apply(messagePort, arguments);
    },
    removeEventListener: function removeEventListener() {
      return messagePort.removeEventListener.apply(messagePort, arguments);
    },
    terminate: function terminate() {
      messagePort.close();
    }
  };
}

export { fromMessagePort };
