import { jsx, Fragment } from 'react/jsx-runtime';
import { memo } from 'react';
import { useAttached } from './hooks.esnext';

function renderText({
  text,
  receiver,
  key
}) {
  return /*#__PURE__*/jsx(RemoteText, {
    text: text,
    receiver: receiver
  }, key);
}
const RemoteText = /*#__PURE__*/memo(({
  text,
  receiver
}) => {
  const attached = useAttached(receiver, text);
  return attached ? /*#__PURE__*/jsx(Fragment, {
    children: attached.text
  }) : null;
});

export { RemoteText, renderText };
