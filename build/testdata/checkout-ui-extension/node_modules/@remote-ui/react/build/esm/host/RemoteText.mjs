import { jsx, Fragment } from 'react/jsx-runtime';
import { memo } from 'react';
import { useAttached } from './hooks.mjs';

function renderText(_ref) {
  var text = _ref.text,
      receiver = _ref.receiver,
      key = _ref.key;
  return /*#__PURE__*/jsx(RemoteText, {
    text: text,
    receiver: receiver
  }, key);
}
var RemoteText = /*#__PURE__*/memo(function (_ref2) {
  var text = _ref2.text,
      receiver = _ref2.receiver;
  var attached = useAttached(receiver, text);
  return attached ? /*#__PURE__*/jsx(Fragment, {
    children: attached.text
  }) : null;
});

export { RemoteText, renderText };
